<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.duan1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ThemTTCTActivity"></activity>
        <activity android:name=".ThemThongTinActivity" />
        <activity android:name=".ThemDichVuActivity" />
        <activity android:name=".ThemVatNuoiActivity" />
        <activity android:name=".QL_ThongTinActivity" />
        <activity android:name=".QL_DichVuActivity" />
        <activity android:name=".QL_VatNuoiActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>